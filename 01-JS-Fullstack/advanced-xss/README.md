# 🧪 Advanced XSS | DOM-based & CSP Bypass

### 🇧🇷 XSS Avançado e Evasão de CSP
Além do `alert(1)`. Este lab foca em injetar scripts através de sinks do DOM (como `location.hash`) e como contornar Content Security Policies mal configuradas através de *Script Gadgets*.

---

### 🇺🇸 Advanced XSS & CSP Evasion
Beyond `alert(1)`. This lab focuses on injecting scripts through DOM sinks (like `location.hash`) and bypassing misconfigured Content Security Policies using Script Gadgets.
