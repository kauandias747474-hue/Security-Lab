# ☕ Lab: Burp Suite Optimization

### 🇧🇷 Diagnóstico: Performance em Auditorias Web
O Burp Suite pode consumir muita memória e ser lento se não for configurado. Este lab foca no ajuste da **JVM (Java Virtual Machine)** e na seleção de extensões que realmente agregam valor sem comprometer a estabilidade.

### 🇺🇸 Diagnostic: Web Audit Performance
Burp Suite can be memory-intensive and slow if not tuned. This lab focuses on **JVM (Java Virtual Machine)** tuning and selecting extensions that add value without compromising stability.

## 🛡️ Configurações Recomendadas
* **Memory Limit:** Ajuste do arquivo `.vmoptions`.
* **Extensions:** `Logger++`, `Turbo Intruder` e `Authorize`.
