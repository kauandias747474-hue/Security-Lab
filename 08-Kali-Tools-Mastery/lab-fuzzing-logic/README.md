# 🔎 Lab: Resource Discovery & Asset Mapping

### 🇧🇷 Objetivo Educativo
Demonstrar a importância de não deixar arquivos sensíveis (como `.env`, `.git` ou backups) em servidores web. Utilizamos o `ffuf` para mostrar como ferramentas automatizadas localizam esses arquivos em segundos.

### 🇺🇸 Educational Objective
Demonstrate the importance of not leaving sensitive files (such as `.env`, `.git`, or backups) on web servers. We use `ffuf` to show how automated tools locate these files in seconds.

## 🛡️ Mitigação Recomendada
* Configuração correta do `.htaccess` ou `nginx.conf`.
* Uso de listas de permissão para acesso a diretórios administrativos.
