# 🐚 Lab: Terminal Mastery (Tmux & ZSH)

### 🇧🇷 Diagnóstico: Multitarefa e Persistência
Em auditorias longas, perder o progresso de um comando ou se perder em múltiplas abas é um risco. Este lab documenta como o **Tmux** permite manter sessões rodando em background e como os **aliases** no `.zshrc` reduzem comandos de 50 caracteres para apenas 3.

### 🇺🇸 Diagnostic: Multitasking & Persistence
In long audits, losing command progress or getting lost in multiple tabs is a risk. This lab documents how **Tmux** keeps sessions running in the background and how **aliases** in `.zshrc` reduce 50-character commands to just 3.

## 🛠️ Conteúdo do Lab
* `.tmux.conf`: Configurações de atalhos e cores.
* `aliases.zsh`: Lista de atalhos personalizados (ex: `nscan` para nmap agressivo).
